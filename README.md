# 柿种 B2B 宣传与销售网站

这是一个面向 B2B 客户的单页营销网站，用于宣传和销售日本零食「柿种」。

适用场景：
- 商超/便利店渠道开发
- 电商与礼盒客户批发合作
- 展会或业务洽谈时的线上展示页

## 文件结构

- `index.html`：页面结构与文案
- `styles.css`：页面样式
- `script.js`：询盘表单校验与提交提示

## 本地预览

在项目根目录运行：

```bash
python3 -m http.server 8080
```

然后访问：

```text
http://localhost:8080
```

## 上线前建议修改

请优先替换 `index.html` 中的以下商务信息：
- 邮箱（`sales@example.com`）
- 电话（`+86 138-0000-0000`）
- 微信（`KAKITANE_B2B`）

如需将询盘真实落地到邮箱或 CRM，可在 `script.js` 中将当前前端提示逻辑改为调用后端 API。